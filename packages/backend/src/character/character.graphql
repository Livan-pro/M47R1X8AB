input CharacterInput {
  name: String
  quenta: Upload
}

type Character {
  id: Int!
  name: String
  quenta: String
}

type Mutation {
  editCharacter(id: Int!, character: CharacterInput!): Boolean
}