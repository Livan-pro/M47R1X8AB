{
  "name": "shared",
  "version": "1.4.0",
  "description": "Matrix's shared stuff",
  "scripts": {
    "lint": "tslint -p tsconfig.json",
    "test": "echo No tests! && exit 0",
    "dev": "tsc -w",
    "build": "rimraf node && tsc && rimraf browser && tsc -p tsconfig.browser.json"
  },
  "keywords": [],
  "author": "Livan",
  "private": true,
  "dependencies": {
    "class-validator": "^0.9.1",
    "typescript": "^3.5.1"
  },
  "devDependencies": {
    "rimraf": "^2.6.3",
    "tslint": "^5.17.0"
  }
}
